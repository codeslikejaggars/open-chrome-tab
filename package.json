{
  "name": "open-chrome-tab",
  "version": "0.2.0",
  "main": "lib/openChromeTab.js",
  "os": [
    "darwin"
  ],
  "license": "ISC",
  "scripts": {
    "lint": "eslint .",
    "compile": "mkdir -p ./lib; babel ./src/openChromeTab.js > ./lib/openChromeTab.js",
    "prepublish": "npm run compile",
    "test": "mocha --compilers js:babel/register --recursive --full-trace"
  },
  "dependencies": {
    "promise": "^7.0.4"
  }
}
